# Core async & HTTP
anthropic>=0.34.0
httpx>=0.27.0

# Dashboard
fastapi>=0.111.0
uvicorn>=0.30.0

# TikTok LIVE chat
TikTokLive>=6.0.0

# TTS — install the one you need
elevenlabs>=1.3.0          # cloud TTS (recommended for quality)
# kokoro>=0.7.0            # local TTS (uncomment if using kokoro backend)
# TTS>=0.22.0              # Coqui TTS (uncomment if using coqui backend)

# Audio / video processing
numpy>=1.26.0
soundfile>=0.12.1
opencv-python-headless>=4.10.0

# Lip sync — choose one and follow its install instructions
# LatentSync: git clone https://github.com/bytedance/LatentSync && pip install -e LatentSync/
# Wav2Lip:    git clone https://github.com/Rudrabha/Wav2Lip && pip install -r Wav2Lip/requirements.txt

# Utilities
python-dotenv>=1.0.0
